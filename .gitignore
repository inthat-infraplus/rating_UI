.venv/
__pycache__/
*.pyc
*.zip
*.egg-info/
dist/
build/
*.egg
*.log

# SQLite DBs (created at runtime, not for git)
*.db
*.db-journal
*.db-wal
*.db-shm

# AI model weights — too large for git (sam2.1_b.pt is ~160 MB).
# Ops downloads them once and either places at <repo-root>/models/sam2.1_b.pt
# or points RATING_UI_SAM2_MODEL at the file.
models/
*.pt
*.pth
*.onnx
sam3-git/
.claude/